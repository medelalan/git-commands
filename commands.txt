move a repo to the local machine:
git clone https://github.com/medelalan/portal.git

see changes in the repo:
git status

stage all change files:
git add .

makes all staged files part of branch:
git commit -m "Adds files to branch"

move changed files to the server:
git push


